# Copy to `.env`, then fill in from Azure Entra → App registrations → your app.

MICROSOFT_TENANT_ID=
MICROSOFT_CLIENT_ID=
# Optional: only if the app is a confidential client (Certificates & secrets → client secret).
# For PKCE/mobile without a secret, enable "Allow public client flows" in Authentication instead.
MICROSOFT_CLIENT_SECRET=
